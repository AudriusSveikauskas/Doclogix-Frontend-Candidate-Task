import React from 'react';
import { Box } from '@mui/material';

const NoFileSelectedPreview = () => {
  console.log('No File Selected Preview');

  return <Box sx={{ paddingY: '60px' }}>no file selected</Box>;
};

export default NoFileSelectedPreview;
