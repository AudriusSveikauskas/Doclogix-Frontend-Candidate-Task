import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#920e8e',
    },
    secondary: {
      main: '#637381',
    },
    text: {
      primary: '#212b36',
      secondary: '#637381',
    },
  },
});

export default theme;
